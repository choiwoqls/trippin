<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pay">
	
	<!-- 희지 -->
	<select id="pay" resultType="pay">
		select * from pay order by pay_seq
	</select>

</mapper>








































